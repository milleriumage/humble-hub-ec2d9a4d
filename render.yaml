services:
  - type: web
    name: imvu-bot-backend
    runtime: node
    plan: free
   buildCommand: npm install && npm run build
    startCommand: node backend/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: DISABLE_IMQ
        value: true
        
      