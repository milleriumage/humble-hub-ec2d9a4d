services:
  - type: web
    name: imvu-bot-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install --omit=dev
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: DISABLE_IMQ
        value: true
